version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - "npm run print:mui"
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths: []